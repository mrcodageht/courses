name: ma premiere action

on: push

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - uses: actions/checkout@v4
      - run: echo "listing apres la recuperation du depot"
      - run: ls