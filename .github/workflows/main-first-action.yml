name: ma premiere action

on: push

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - uses: actions/checkout@v6
      - run: ls
      - run: echo ${{ github.workspace }}
      - uses: actions/checkout@v6
        with:
          repository: 'mrcodageht/ftp-module'
      - run: ls
      - name: step event name
        run: echo ${{ github.event_name }}
